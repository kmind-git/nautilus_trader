# 订单中枢（Order Hub）

由 NautilusTrader 改造而来的买方订单管理系统：策略团队通过 API 提交订单，
系统统一执行风控、路由到交易场所、跟踪订单全生命周期。

## Language

**订单中枢**:
本系统的名称。面向策略团队的内部买方 OMS，不是券商柜台，不面向终端客户，不含清结算。
_Avoid_: OMS（与 NautilusTrader 既有的 `OmsType` 概念冲突，见下）

**OmsType**:
NautilusTrader 原有概念，指持仓记账模式（Netting 净额 / Hedging 锁仓），与"订单管理系统"无关。改造中保留原义，不复用该词指代本系统。
_Avoid_: 用 OMS 一词同时指代两者

**提交方（Submitter）**:
通过 API 向订单中枢提交订单的策略或交易员，分两类：策略程序（自动下单）与交易端（手动下单）。每个提交方有独立身份，映射为系统内的策略身份，限额与归因各自独立。
_Avoid_: 用户、客户（本系统不面向终端客户）

**主体（Trader）**:
订单中枢服务的唯一交易身份（TraderId）。多场所账户归属同一主体。
_Avoid_: 租户、多主体（首期只有一个主体）

**风控拦截（Pre-trade Check）**:
订单提交时的规则校验（限额、频率、价格带），不通过即拒单。全系统无人工审批环节，事后审计依赖事件日志。
_Avoid_: 审批（不存在人工审批状态）

**行情（Market Data）**:
仅供内核内部消费的数据（持仓盯市、条件单触发等），按需订阅。订单中枢不向提交方分发行情；交易端与策略程序自备数据源。
_Avoid_: 行情服务、行情下发（中枢不提供此能力）

**场所（Venue）**:
交易所或经纪商，通过适配器接入；一个主体可在多个场所持有账户。

**合约（Instrument）**:
场所内可交易的品种定义（价格步进、数量步进、最小名义额等）。提交方构造订单的合法前提。
_Avoid_: 标的、品种（统称合约）

**交易端（Trading Desk）**:
交易员使用的 Tauri + Rust 桌面客户端，是订单中枢的提交方之一。
_Avoid_: 前端、控制台（指客户端整体，而非其某个页面）
