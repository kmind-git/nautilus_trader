---
name: orderhub-oms-transformation
description: NautilusTrader fork 改造为订单中枢的设计方案、流程契约与实施门槛
metadata:
  node_type: memory
  type: project
  originSessionId: sess_717fac57-553b-4e3d-b08e-1272876b3092
---

# 订单中枢改造方案

修订日期：2026-09-20。状态：设计评审稿，尚未实现或通过运行验证。
代码核对基线：`develop`，`9b2df89ef3ff2f636080fd2a18b3f907a6217158`。

本方案将 NautilusTrader fork 改造为策略团队的买方订单中枢，统一处理订单准入、
风控、路由、生命周期和状态恢复。术语沿用 [CONTEXT.md](CONTEXT.md)。
以下区分既有产品边界、本次设计建议和待验证条件；文档中的状态名及字段是拟议协议。

## 产品与工程边界

- 定位：内部买方订单管理系统，单个 `TraderId`，支持多个交易通道，不承担清结算。
- 提交方：纯 Rust 策略程序和 Tauri + Rust 交易端。后端独立交付，交易端并行开发。
- 首期资产方向：股票，以美股为目标。首个执行通道尚未指定，候选及限制见
  [美股接入与容量](docs/orderhub/venues-and-capacity.md)。
- 容量需求：每秒 1000 笔提交和 1000 笔撤单。接入峰值、持续时间及实盘发出速率的口径需分别明确，
  不把系统压测成绩当作券商通道能力。
- 协议：gRPC + tonic，仅保留 `SubmitOrder`、`CancelOrder`、`GetState`、`WatchEvents` 四个业务 RPC。
- 订单范围：`MARKET`、`LIMIT`；原候选有效期为 `GTC`、`IOC`、`FOK`，美股新增 `DAY` 候选待选择。
  `post_only`、`reduce_only` 按通道能力验证；不承诺这些选项的任意组合。
- 暂无 `ModifyOrder`。撤单重下需要等待撤单结果并计入期间成交，不提供原子替换承诺。
- API 不分发行情；中枢内部仍消费风控、估值和 Sandbox 撮合所需的行情，客户端自接行情源。
- 风控采用规则拦截，无人工审批；运维停单、异常恢复决策不属于逐笔审批。
- 不引入 Redis/Postgres。新增本地持久化边界的候选为 redb，必须先验证故障恢复和写入吞吐。
- Fork 以新增模块为主，保留上游代码；固定实施 SHA，后续按发布 tag 升级，急需修复单独评估 cherry-pick。
  当前核对基线为 develop，尚未选定可用于生产的发布基线。
- 交易端开发、对外分发时的 LGPL 评估继续单独处理；本次不承诺高可用集群和跨节点一致性。

## 文档导航

| 文档                                                    | 解决的问题                      |
| ----------------------------------------------------- | -------------------------- |
| [受理与幂等](docs/orderhub/admission-and-idempotency.md)   | 超时重试、持久受理、订单 ID、撤单竞争与结果查询。 |
| [限额与预占](docs/orderhub/risk-and-reservations.md)       | 提交方身份、策略与账户额度、精确计算、通道限流。   |
| [快照与补流](docs/orderhub/state-and-event-stream.md)      | 一致性快照、终态查询、跨重启游标、慢消费者。     |
| [重启与对账](docs/orderhub/recovery-and-reconciliation.md) | 本地落盘、故障阻断、不确定订单、归因与恢复交易。   |
| [美股接入与容量](docs/orderhub/venues-and-capacity.md)       | 现成适配器、交易所接口、吞吐目标及压测口径。     |

每份流程契约都列出验收场景。这些场景是实施要求，不代表测试已经完成。

## 模块与运行边界

保留两个业务 crate 和一个协议 crate，不为本轮设计新增第四个 crate：

| 模块                 | 职责                                              |
| ------------------ | ----------------------------------------------- |
| `orderhub-proto`   | proto 与生成代码，无 Nautilus 依赖；后端、策略和交易端共用。          |
| `orderhub-gateway` | tonic 服务、身份校验、桥接 Actor、幂等与限额模块、本地业务日志、状态投影与事件流。 |
| `orderhub-node`    | 配置与依赖组装、LiveNode 生命周期、适配器注册、运行就绪与故障控制。          |

```text
提交方 -> tonic -> 有界请求通道 -> 核心线程中的桥接组件
                                  | 身份、幂等、规则、预占
                                  | 异步提交业务日志，收到提交确认后推进
                                  v
                           订单初始化与 Cache 登记
                                  v
                           RiskEngine -> ExecutionEngine -> 执行通道
                                  ^                         |
                                  |       订单/成交/账户回报 |
                                  +-------------------------+
核心状态变化 -> 持久化业务事件 -> 一致性查询投影 -> WatchEvents
```

核心线程拥有 Nautilus Cache 和 MessageBus 的访问权。tonic 任务只交换可跨线程的值对象，
通过回复通道获取结果。核心回调不能等待网络发送、磁盘提交或慢客户端。
持久化工作线程通过提交确认消息驱动下一阶段；等待期间的暂占额度仍计入可用额度。

直接转发 `SubmitOrder` 不足以完成桥接：现有 RiskEngine 要求订单已在 Cache 中。
M0 必须验证初始化事件、Cache 登记、风险端点调用、拒单回传及归因的完整路径。
复用 LiveNode 的连接和对账生命周期，不以裸 runner 代替实盘节点生命周期。

## 持久化权责

建议在 gateway 内实现小型 OrderHub 业务日志，使用独立的本地 redb 存储空间，
负责请求受理记录、幂等索引、额度变更、待发送意图、订单归属及对外事件。
它与 Nautilus event_store 分工如下：

| 状态                     | 权责                                |
| ---------------------- | --------------------------------- |
| 外部实际订单、成交、账户           | 执行通道回报与覆盖完整的对账结果是外部事实依据。          |
| 提交方身份、请求是否受理、策略归属、额度预占 | OrderHub 业务日志是本地恢复依据。             |
| Nautilus 引擎事件历史        | 现有 event_store 用于捕获、检查和经过验证的状态回放。 |
| GetState 与 WatchEvents | 从已提交的 OrderHub 状态投影及事件读取。         |

这不是跨交易所事务：本地提交与远端接单之间始终可能出现不确定窗口。
业务日志与 event_store 也没有原子双写保证，恢复时必须用稳定 ID 关联、去重和对账。
M0 若验证不能满足以上契约，应调整存储实现，不通过取消持久确认降低受理保证。

## 里程碑与验收门槛

| 阶段            | 交付                                        | 通过门槛                                         |
| ------------- | ----------------------------------------- | -------------------------------------------- |
| M0：关键验证       | 固定 SHA、Rust 桥接原型、本地日志与快照验证、通道选型。          | 证明主线程桥接可行；崩溃窗口可识别；写入成本和限流边界有实测。              |
| M1：Sandbox 闭环 | 四个 RPC、持久幂等、基础额度模块、快照与补流、确定性行情输入、脚本验收。    | 四份契约的基础场景通过；1000 提交/s + 1000 撤单/s 按独立容量计划测试。 |
| M2：实盘试运行前     | 每提交方鉴权、完整限额、目标适配器能力表、故障恢复、账户与策略对账、业务就绪状态。 | 全部故障场景通过；目标券商回报语义、限流与归因恢复获得验证。               |
| M3：生产运行       | 监控告警、备份恢复、容量和延迟 SLO、升级回滚、停单流程、单活防护。       | 恢复演练可复现，磁盘与事件保留容量充分，生产基线及通道配额确定。             |

M1 可使用隔离 Sandbox 内的共享静态 token；此配置不具备提交方身份隔离保证。
真实执行通道启用前必须使用服务端凭证映射，控制下单、撤单、查询和订阅权限。
明文 gRPC 仅作为受控本机或加密 VPN 内的 M1 联调配置；部署前明确 TLS 或加密隧道边界，
未完成身份与传输保护配置时不启用真实交易。

## 实施前待定项

| 项目                | 当前状态                                            |
| ----------------- | ----------------------------------------------- |
| 首个券商/交易所通道        | 未选定；建议 IBKR Paper 用于功能验证，高吞吐实盘通道另行选择。           |
| 1000 + 1000 的速率口径 | 数值已提出；系统峰值与实盘持续速率的含义待确认。                        |
| 延迟与容量             | P99 受理延迟、最大排队时间、提交方数、合约数、活跃订单数、行情输入速率待定。        |
| 美股首期边界            | 建议整股、USD、常规交易时段、买入及卖出现有持仓；融资、卖空、碎股和盘前盘后待明确。     |
| 有效期与交易能力          | `DAY`、`post_only`、`reduce_only` 的业务需要与通道支持逐项确认。 |
| 持仓归属              | 建议首期一个账户合约组合只由一个策略管理；共享 NETTING 的归因恢复另设验收。      |
| 恢复与保留             | RTO、备份 RPO、日志/事件/终态保留周期、恢复权限及数据目录待定。            |

待定项不阻止通用协议及 Sandbox 工作；涉及真实通道、安全边界和生产性能的实现依赖其明确。

## 本次修订依据

- [RiskEngine 配置](crates/risk/src/engine/config.rs)：已有引擎级限频及按合约单笔名义额配置，需另建提交方额度账本。
- [RiskEngine 提交路径](crates/risk/src/engine/mod.rs)：`RiskEngine::handle_submit_order` 依赖 Cache 中已登记订单。
- [线程模型](docs/concepts/architecture.md)：核心状态在节点线程中串行访问。
- [Event sourcing](docs/concepts/event_sourcing.md)：异步捕获、halt 行为、Cache 快照归属与回放边界。
- [Cache 写入](crates/common/src/cache/mod.rs)：`Cache::add` 仅在配置数据库适配器时调用持久化。
- [内核启动](crates/system/src/kernel.rs)：显式回放模式跳过实盘启动与对账。
- [执行对账](docs/concepts/execution/reconciliation.md)：共享 NETTING 的外部净持仓不能单独恢复策略分配。

原方案第 6、7、9、11 项分别补充了额度预占、持久受理与恢复、RPC 一致性语义和实盘前身份绑定。
保留原始项目元数据，移除“已经定稿且可直接实施”的表述，避免将设计建议误当作现有能力。
